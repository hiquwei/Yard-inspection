# OCR诊断版使用说明

1. 上传本目录全部内容到 GitHub 仓库根目录，确保 `.github/workflows/pages.yml` 和 `scripts/build-site.sh` 存在。
2. GitHub Pages 的 Source 选择 **GitHub Actions**。
3. 部署成功后，用 Safari 打开 Pages 地址。
4. 页面顶部应显示版本 `2026.07.28-diagnostic-1`。
5. 先生成箱号范围，再点击“打开测试卡”确认测试号码为 `TXGU 565884 1`。
6. 拍摄失败后，诊断窗口自动显示：实际裁剪图、图像处理结果、各次OCR原始文本、候选匹配与裁剪坐标。
7. 点击“导出诊断 JSON”可导出不含图片的诊断报告；点击“下载裁剪图”可单独保存实际裁剪图。
8. 如果手机仍显示旧版本，点击“清缓存并更新”。

诊断图片只保存在当前页面内存中，不写入 localStorage，也不进入检验记录。
