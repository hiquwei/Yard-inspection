# 部署说明（中文）

## 一、上传前

请先解压 ZIP。仓库根目录最终应看到：

```text
index.html
manifest.webmanifest
sw.js
.nojekyll
icons/
scripts/
.github/workflows/pages.yml
```

不要只上传 `index.html`，也不要把整个文件夹再套在另一层同名文件夹中。

## 二、创建仓库

1. 登录 GitHub，点击右上角 `+`，选择 `New repository`。
2. 名称可填 `container-inspection`。
3. 建议选择 `Public`，然后点击 `Create repository`。

## 三、上传文件

进入仓库后点击 `Add file → Upload files`，上传解压后的所有内容并提交。

注意：网页上传时容易漏掉以点开头的 `.github` 和 `.nojekyll`。上传完成后，请确认仓库中能看到 `.github/workflows/pages.yml`。如果浏览器不方便上传隐藏文件，建议使用 GitHub Desktop。

## 四、启用 GitHub Pages

1. 打开 `Settings`。
2. 左侧选择 `Pages`。
3. 在 `Build and deployment` 中将 `Source` 设为 `GitHub Actions`。

## 五、等待自动部署

打开仓库顶部的 `Actions`，选择 `Deploy Container Inspection OCR`。第一次运行会下载大约数十 MB 的 OCR 文件，通常需要几分钟。

成功标志：

- Build 步骤为绿色；
- Deploy 步骤为绿色；
- `Settings → Pages` 显示 HTTPS 网站地址。

如果 Actions 失败，点开红色步骤查看错误。常见原因是临时网络下载失败；可点击 `Re-run jobs` 重试。

## 六、在 iPhone 上使用

1. 复制 GitHub Pages 的 `https://...` 地址。
2. 在 Safari 地址栏直接打开，不要用文件预览、微信内置预览或打印软件预览。
3. 页面打开后会在后台准备本地 OCR；顶部应显示“本地 OCR 已就绪”。
4. 点击“打开连续识别相机”，相机权限选择“允许”。
5. 可将网页添加到主屏幕。

## 七、如何判断部署是否完整

在网站地址后分别访问：

```text
ocr/version.json
ocr/tesseract.min.js
ocr/worker.min.js
ocr/lang/eng.traineddata.gz
```

`version.json` 应直接显示版本信息。其他文件会显示或下载，不能是 404。

## 八、隐私与备份

- 照片只在浏览器内用于当次 OCR，不写入检验记录。
- 状态、日期、备注保存在当前 Safari 的本地存储。
- GitHub 上只有网页程序和 OCR 模型，没有现场照片和检验数据。
- 定期导出 CSV，避免清理浏览器数据造成记录丢失。
